# WL OPS Frontend — copy to .env.local for development

# API base URL — in production this is your server's address
# In dev, Vite proxies /api to the backend (defined in vite.config.js)
VITE_API_BASE_URL=/api

# App metadata
VITE_APP_NAME=Well Land Ops
VITE_APP_VERSION=2.0.0
